const USER_AGENT = 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36'; async function test() { const res = await fetch('https://www.tiktok.com/@karam.drame/live', { headers: { 'User-Agent': USER_AGENT, 'Accept': 'text/html', 'Cache-Control': 'no-cache' } }); const html = await res.text(); const idx = html.indexOf('__UNIVERSAL_DATA_FOR_REHYDRATION__'); if (idx !== -1) { const tagClose = html.indexOf('>', idx); const endTag = html.indexOf('</script>', tagClose); const json = JSON.parse(html.substring(tagClose + 1, endTag)); console.log('KEYS:', Object.keys(json.__DEFAULT_SCOPE__ || {})); for (const k of Object.keys(json.__DEFAULT_SCOPE__ || {})) { console.log(k, JSON.stringify(json.__DEFAULT_SCOPE__[k]).substring(0, 150)); } } } test();