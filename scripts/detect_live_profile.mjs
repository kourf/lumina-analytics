const USER_AGENT = 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36'; async function detect() { const profileRes = await fetch('https://www.tiktok.com/@karam.drame', { headers: { 'User-Agent': USER_AGENT, 'Accept': 'text/html', 'Accept-Language': 'fr-FR,fr;q=0.9', 'Cache-Control': 'no-cache' } }); const html = await profileRes.text(); const idx = html.indexOf('__UNIVERSAL_DATA_FOR_REHYDRATION__'); if (idx !== -1) { const tagClose = html.indexOf('>', idx); const endTag = html.indexOf('</script>', tagClose); const json = JSON.parse(html.substring(tagClose + 1, endTag)); const userInfo = json.__DEFAULT_SCOPE__?.['webapp.user-detail']?.userInfo?.user; console.log('User Info roomId:', userInfo?.roomId); if (userInfo?.roomId && userInfo.roomId !== '0') { const roomRes = await fetch('https://webcast.tiktok.com/webcast/room/info/?aid=1988&room_id=' + userInfo.roomId, { headers: { 'User-Agent': USER_AGENT, 'Accept': 'application/json', 'Referer': 'https://www.tiktok.com/@karam.drame' } }); const rJson = await roomRes.json(); const r = rJson.data; console.log('STATUS:', r?.status, 'TITLE:', r?.title, 'VIEWERS:', r?.user_count, 'STATS:', r?.stats); } } } detect();